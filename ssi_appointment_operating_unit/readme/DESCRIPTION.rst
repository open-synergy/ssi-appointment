Glue module that adds Operating Unit support to the Appointment module.
Extends ``appointment_schedule`` and ``appointment_request`` with
``mixin.single_operating_unit``, adding the ``operating_unit_id`` field,
security groups/rules, and view integration.
